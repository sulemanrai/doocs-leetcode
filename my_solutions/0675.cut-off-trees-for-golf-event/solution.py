class Solution:
    def cutOffTree(self, forest: List[List[int]]) -> int:
        def f(i, j, x, y):
            # TODO: implement
            pass
        def bfs(i, j, x, y):
            # TODO: implement
            pass
        m, n = len(forest), len(forest[0])
