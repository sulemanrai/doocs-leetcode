#include <vector>
using namespace std;

class Node {
public:
    int l, r;
    int g;

    Node(int l, int r)
        : l(l)
        , r(r)
        , g(0) {
            // TODO: implement
        }
};
