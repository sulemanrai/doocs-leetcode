class Solution:
    def shipWithinDays(self, weights: List[int], days: int) -> int:
        def check(mx):
            # TODO: implement
            pass
        left, right = max(weights), sum(weights) + 1
