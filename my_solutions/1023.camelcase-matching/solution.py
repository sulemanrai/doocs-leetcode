class Solution:
    def camelMatch(self, queries: List[str], pattern: str) -> List[bool]:
        def check(s, t):
            # TODO: implement
            pass
        return [check(q, pattern) for q in queries]
