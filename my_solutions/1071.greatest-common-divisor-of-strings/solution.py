class Solution:
    def gcdOfStrings(self, str1: str, str2: str) -> str:
        def check(a, b):
            # TODO: implement
            pass
        for i in range(min(len(str1), len(str2)), 0, -1):
